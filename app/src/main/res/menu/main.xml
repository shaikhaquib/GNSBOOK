<?xml version="1.0" encoding="utf-8"?>
<menu
  xmlns:android="http://schemas.android.com/apk/res/android" xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:icon="@drawable/ic_add_512" android:id="@+id/mnAdd" android:visible="false" android:orderInCategory="100" android:title="Add Money" app:showAsAction="ifRoom" />
    <item android:id="@+id/action_search"
        android:title="Search"
        android:icon="@drawable/ic_magnifying_glass_icon"
        app:showAsAction="ifRoom"
        />
    <item android:id="@+id/cart"
        android:title="Cart"
        android:icon="@drawable/ic_shopping_cart"
        app:showAsAction="ifRoom"
        />
</menu>